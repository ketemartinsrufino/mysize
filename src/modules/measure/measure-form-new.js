import React  from 'react';

const MeasureFormNew = ( { jsx, save } ) => {

    return jsx(<button className="mdc-button mdc-button--raised" onClick={save}>Salvar</button>) ;
};

export default MeasureFormNew;